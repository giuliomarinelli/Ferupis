import { component$ } from "@builder.io/qwik";

export const MillefioriSubtitle = component$(() => (
  <p class="subtitle">
    Un miele che cambia con il territorio, le fioriture e la stagione.
  </p>
));
