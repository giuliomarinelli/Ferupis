import { component$ } from "@builder.io/qwik";

export const IlMieleSubtitle = component$(() => (
  <p class="mt-5 max-w-xl text-base leading-7 opacity-75 sm:text-lg">
    Dalla raccolta del nettare alla maturazione nel favo.
  </p>
));
