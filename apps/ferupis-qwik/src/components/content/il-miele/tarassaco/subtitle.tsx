import { component$ } from "@builder.io/qwik";

export const TarassacoSubtitle = component$(() => (
  <p class="subtitle">
    Un miele primaverile dal gusto caratteristico e dalla cristallizzazione rapida.
  </p>
));
